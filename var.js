var pet = '🐶';
function setPet() {
    var pet = '😽';
    console.log("Pet in the house : " + pet);
}
setPet();
console.log("Pet outside the house : " + pet);
var index = 0;
for (var indexInternal = 0; indexInternal < 5; indexInternal++) {
    console.log("Index inside the for loop : " + indexInternal);
}
console.log("Index outside the for loop : " + index);
